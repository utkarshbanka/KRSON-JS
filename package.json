{
  "name": "krson-js",
  "version": "2.1.0",
  "description": "Binary format faster than JSON for single-field and multi-field access. 10x faster field reads, 26% smaller payloads, zero native dependencies.",
  "main": "index.js",
  "scripts": {
    "test": "node test.js"
  },
  "keywords": ["binary", "serialization", "json", "format", "performance", "schema"],
  "license": "MIT",
  "files": ["index.js", "README.md"],
  "engines": {
    "node": ">=14"
  }
}
